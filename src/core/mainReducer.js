import { ReactEnt } from '../';

// TODO: Figure out a way to combine reducers
const combineReducers = () => {
  let combinedReducers;

  return combinedReducers;
};

export const mainReducer = combineReducers();
